// import {setTimeout as time} from '../common/utils/time';
// import * as sinon_module from 'ts-sinon';
// const sinon = sinon_module.default;

//This is to ensure that the test packages are working as intended
describe('Sample Test', () => {
  it('should test that true === true', () => {
    expect(true).toBe(true)
  })
})
